node_modules/
dist/
dist-electron/
coverage/
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.local
*.tsbuildinfo
# Note: do NOT ignore "data/" globally — server/src/data/ has WO-005 platform-selectors
# and WO-008 action-selectors JSON configs. Only ignore the production runtime data dir:
/data/
logs/
*.key
master.key

# Local dev PostgreSQL (not for prod)
.dev-pg/
.dev-data/

# Master audit scratch (smoke scripts, Electron user data partitions)
.audit-tmp/
